/* var Authenticate = require('./clients/watson/config/authenticate');

function Validation() {}

Validation.prototype.Auth = function(req, res, next) {
    if(visaTokenValidation){
        next(req, res, next);
    }else{
        Authenticate.Login(req, res, next);
    }    
};

module.exports = new Validation(); */